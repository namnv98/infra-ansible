[Unit]
Description=etcd key-value store
After=network.target

[Service]
User={{ etcd_user }}
ExecStart={{ etcd_install_dir }}/etcd --config-file={{ etcd_conf_dir }}/etcd.conf.yml
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
